# Exactly the third-party packages reachable from `lighthouse/serve/app.py`, pinned to the
# versions the service was verified against on 2026-08-13.
#
# `datasets`, `pandas`, `matplotlib` and `anthropic` are in `ml/pyproject.toml` and are
# deliberately absent here: nothing on the serve path imports them. The import closure of
# `serve/app.py` is 12 lighthouse modules and the seven packages below.
#
# torch is installed separately in the Dockerfile, from the CPU index. It is listed here
# for the record and is already satisfied by the time pip reads this file.
torch==2.13.0
transformers==5.14.1
numpy==2.5.1
scikit-learn==1.9.0
fastapi==0.141.1
uvicorn[standard]==0.52.1
pydantic==2.13.4
